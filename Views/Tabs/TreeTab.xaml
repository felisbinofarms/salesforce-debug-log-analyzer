<UserControl x:Class="SalesforceDebugAnalyzer.Views.Tabs.TreeTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:SalesforceDebugAnalyzer.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="900">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Column headers -->
        <Border Grid.Row="0" Background="{StaticResource BgTertiary}"
                BorderBrush="{StaticResource BorderSubtle}" BorderThickness="0,0,0,1"
                Padding="20,8,20,8"
                Visibility="{Binding HasExecutionTree, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="70"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="" Width="26"/>
                <TextBlock Grid.Column="1" Text="METHOD" FontSize="11" FontWeight="SemiBold"
                           Foreground="{StaticResource TextMuted}"/>
                <TextBlock Grid.Column="2" Text="TYPE" FontSize="11" FontWeight="SemiBold"
                           Foreground="{StaticResource TextMuted}" TextAlignment="Center"/>
                <TextBlock Grid.Column="3" Text="TIME" FontSize="11" FontWeight="SemiBold"
                           Foreground="{StaticResource TextMuted}" TextAlignment="Right"/>
            </Grid>
        </Border>

        <!-- Tree Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="4,4">
            <Grid>
                <TreeView ItemsSource="{Binding ExecutionTreeNodes}" Background="Transparent" BorderThickness="0"
                          Foreground="{StaticResource TextPrimary}"
                          Visibility="{Binding HasExecutionTree, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TreeView.Resources>
                        <HierarchicalDataTemplate DataType="{x:Type viewmodels:TreeNodeViewModel}"
                                                  ItemsSource="{Binding Children}">
                            <Border CornerRadius="4" Padding="8,5" Margin="0,1">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{StaticResource BgHover}"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding TypeIcon}" FontSize="13"
                                               VerticalAlignment="Center" Margin="0,0,6,0"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="12"
                                               Foreground="{StaticResource TextPrimary}" TextTrimming="CharacterEllipsis"
                                               VerticalAlignment="Center"
                                               FontFamily="{StaticResource FontMono}"/>
                                    <Border Grid.Column="2" CornerRadius="3" Padding="5,2" Margin="8,0">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="{StaticResource BgTertiary}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding TypeName}" Value="Soql">
                                                        <Setter Property="Background" Value="{StaticResource AccentSubtle}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding TypeName}" Value="Dml">
                                                        <Setter Property="Background" Value="#F9731630"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding TypeName}" Value="Exception">
                                                        <Setter Property="Background" Value="{StaticResource DangerSubtle}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding TypeName}" FontSize="10"
                                                   Foreground="{StaticResource TextSecondary}"/>
                                    </Border>
                                    <TextBlock Grid.Column="3" Text="{Binding DurationDisplay}" FontSize="12"
                                               FontWeight="SemiBold" VerticalAlignment="Center"
                                               MinWidth="55" TextAlignment="Right"
                                               FontFamily="{StaticResource FontMono}">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding TypeName}" Value="Soql">
                                                        <Setter Property="Foreground" Value="{StaticResource AccentPrimary}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding TypeName}" Value="Dml">
                                                        <Setter Property="Foreground" Value="{StaticResource Warning}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </Border>
                        </HierarchicalDataTemplate>
                    </TreeView.Resources>
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="TreeViewItem">
                            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                            <Setter Property="FontSize" Value="12"/>
                        </Style>
                    </TreeView.ItemContainerStyle>
                </TreeView>

                <!-- Empty state -->
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,60,0,0"
                            Visibility="{Binding HasExecutionTree, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                    <materialDesign:PackIcon Kind="FileTreeOutline" Width="32" Height="32"
                                             Foreground="{StaticResource TextDisabled}"
                                             HorizontalAlignment="Center" Margin="0,0,0,12"/>
                    <TextBlock Text="No execution tree data" FontSize="13" FontWeight="SemiBold"
                               Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                    <TextBlock Text="Select a log to see how code was executed"
                               FontSize="12" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>