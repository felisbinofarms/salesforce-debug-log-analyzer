<Window x:Class="SalesforceDebugAnalyzer.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:helpers="clr-namespace:SalesforceDebugAnalyzer.Helpers"
        xmlns:viewmodels="clr-namespace:SalesforceDebugAnalyzer.ViewModels"
        xmlns:views="clr-namespace:SalesforceDebugAnalyzer.Views"
        xmlns:tabs="clr-namespace:SalesforceDebugAnalyzer.Views.Tabs"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="Salesforce Debug Log Analyzer" 
        Height="900" 
        Width="1600"
        MinHeight="700"
        MinWidth="1200"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        WindowStyle="None"
        AllowsTransparency="False"
        ResizeMode="CanResize"
        Background="{StaticResource BgPrimary}"
        TextElement.Foreground="{StaticResource TextPrimary}"
        FontFamily="Segoe UI, Arial"
        TextElement.FontSize="{StaticResource FontBaseline}">
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="48"
                      ResizeBorderThickness="5"
                      GlassFrameThickness="0"
                      CornerRadius="8"
                      UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>
    
    <Window.Resources>
        
        <!-- Card Style — uses theme tokens so all cards inherit the palette automatically -->
        <Style x:Key="DiscordCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource BgSecondary}"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderDefault}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- Aliases for existing global styles (backward compatibility) -->
        <Style x:Key="DiscordPrimaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}"/>
        <Style x:Key="DiscordSecondaryButton" TargetType="Button" BasedOn="{StaticResource SecondaryButton}"/>

        <!-- Pill CTA Button for links -->
        <Style x:Key="PillLinkButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Info}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="12" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentActive}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- CheckBox using theme colors -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="CheckBox" Width="18" Height="18"
                                    Background="{StaticResource BgInput}" BorderBrush="{StaticResource BorderFocus}"
                                    BorderThickness="2" CornerRadius="3" Margin="0,0,8,0">
                                <Path x:Name="CheckMark"
                                      Data="M 2,5 L 7,10 L 14,2"
                                      Stroke="{StaticResource AccentPrimary}" StrokeThickness="2"
                                      Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter Grid.Column="1" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="CheckBox" Property="Background" Value="{StaticResource AccentPrimary}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBox" Property="BorderBrush" Value="{StaticResource AccentHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Background="{StaticResource BgPrimary}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="56"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="26"/>
        </Grid.RowDefinitions>
        
        <!-- Custom Title Bar (48px) -->
        <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                Background="{StaticResource SidebarBg}"
                BorderBrush="{StaticResource BorderSubtle}" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="56"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- App Icon Area -->
                <Border Grid.Column="0" Background="{StaticResource SidebarBg}">
                    <materialDesign:PackIcon Kind="Spider" Width="22" Height="22"
                                             VerticalAlignment="Center" HorizontalAlignment="Center"
                                             Foreground="{StaticResource AccentPrimary}"/>
                </Border>
                
                <!-- App Title + Context Breadcrumb -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0,0,0">
                    <TextBlock Text="Black Widow" FontSize="14" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimary}"/>
                    <!-- Breadcrumb separator + context (shown when a log is selected) -->
                    <TextBlock Text=" › " FontSize="13" Foreground="{StaticResource TextMuted}"
                               Visibility="{Binding SelectedLog, Converter={StaticResource NullToVisibilityConverter}}"/>
                    <TextBlock Text="{Binding SelectedLog.OperationType}" FontSize="13"
                               Foreground="{StaticResource TextSecondary}"
                               TextTrimming="CharacterEllipsis" MaxWidth="300"
                               Visibility="{Binding SelectedLog, Converter={StaticResource NullToVisibilityConverter}}"
                               ToolTip="{Binding SelectedLog.EntryPoint}"/>
                </StackPanel>
                
                <!-- Streaming Status Badge (compact, shown only when streaming) -->
                <Border Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center"
                        Background="{StaticResource Danger}" CornerRadius="10" Padding="10,4"
                        Visibility="{Binding IsStreaming, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="6" Height="6" Fill="White" Margin="0,0,6,0">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsStreaming}" Value="True">
                                            <DataTrigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard RepeatBehavior="Forever">
                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                         From="1" To="0.3" Duration="0:0:0.5"
                                                                         AutoReverse="True"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.EnterActions>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock Text="{Binding StreamingStatus}" 
                                   Foreground="White" FontSize="11" FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>
                
                <!-- Window Controls (Right) -->
                <StackPanel Grid.Column="3" Orientation="Horizontal"
                            WindowChrome.IsHitTestVisibleInChrome="True">
                    <!-- Alert Bell Button -->
                    <Button Command="{Binding ToggleAlertCenterCommand}"
                            Width="48" Height="48"
                            Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"
                            Background="Transparent" BorderThickness="0"
                            ToolTip="Alerts">
                        <Grid>
                            <materialDesign:PackIcon Kind="BellOutline"
                                                     Foreground="{StaticResource TextSecondary}" Width="18" Height="18"
                                                     HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <!-- Unread badge -->
                            <Border Background="{StaticResource Danger}" CornerRadius="7"
                                    MinWidth="14" Height="14" Padding="3,0"
                                    HorizontalAlignment="Right" VerticalAlignment="Top"
                                    Margin="0,8,6,0"
                                    Visibility="{Binding UnreadAlertCount, Converter={StaticResource GreaterThanZeroToVisibilityConverter}}">
                                <TextBlock Text="{Binding UnreadAlertCount}" FontSize="9" FontWeight="Bold"
                                           Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </Button>
                    <Button Click="MinimizeButton_Click"
                            Width="48" Height="48"
                            Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"
                            Background="Transparent" BorderThickness="0"
                            ToolTip="Minimize">
                        <materialDesign:PackIcon Kind="WindowMinimize"
                                                 Foreground="{StaticResource TextSecondary}" Width="16" Height="16"/>
                    </Button>
                    <Button Click="MaximizeButton_Click"
                            Width="48" Height="48"
                            Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"
                            Background="Transparent" BorderThickness="0"
                            ToolTip="Maximize">
                        <materialDesign:PackIcon Kind="WindowMaximize"
                                                 Foreground="{StaticResource TextSecondary}" Width="16" Height="16"/>
                    </Button>
                    <Button Click="CloseButton_Click"
                            Width="48" Height="48"
                            Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"
                            Background="Transparent" BorderThickness="0"
                            ToolTip="Close">
                        <materialDesign:PackIcon Kind="Close" 
                                                 Foreground="{StaticResource TextSecondary}" Width="16" Height="16"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Left Sidebar (56px icon rail) -->
        <Border Grid.Row="1" Grid.Column="0" 
                Background="{StaticResource SidebarBg}" 
                BorderBrush="{StaticResource BorderSubtle}" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Navigation Icons (Top) -->
                <StackPanel Grid.Row="0" Margin="0,12,0,0" HorizontalAlignment="Center">
                    <!-- Home -->
                    <Button Width="44" Height="44"
                            Background="{StaticResource AccentPrimary}"
                            BorderThickness="0" Margin="0,0,0,6" Cursor="Hand"
                            ToolTip="Home"
                            Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10"/>
                            </Style>
                        </Button.Resources>
                        <materialDesign:PackIcon Kind="Home" Foreground="White" Width="24" Height="24"/>
                    </Button>
                    
                    <!-- Upload File -->
                    <Button Width="44" Height="44"
                            Background="Transparent" BorderThickness="0"
                            Margin="0,0,0,6" Cursor="Hand"
                            Command="{Binding UploadLogCommand}"
                            ToolTip="Upload log file (Ctrl+O)"
                            Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10"/>
                            </Style>
                        </Button.Resources>
                        <materialDesign:PackIcon Kind="Upload" Foreground="{StaticResource TextSecondary}" Width="24" Height="24"/>
                    </Button>
                    
                    <!-- Load Folder -->
                    <Button Width="44" Height="44"
                            Background="Transparent" BorderThickness="0"
                            Margin="0,0,0,6" Cursor="Hand"
                            Command="{Binding LoadLogFolderCommand}"
                            ToolTip="Load log folder"
                            Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10"/>
                            </Style>
                        </Button.Resources>
                        <materialDesign:PackIcon Kind="FolderOpen" Foreground="{StaticResource TextSecondary}" Width="24" Height="24"/>
                    </Button>
                    
                    <!-- Go Live (Stream) -->
                    <Button Width="44" Height="44" BorderThickness="0"
                            Margin="0,4,0,6" Cursor="Hand"
                            Command="{Binding ToggleStreamingCommand}"
                            ToolTip="Stream live logs from org">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10"/>
                            </Style>
                        </Button.Resources>
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource {x:Static ToolBar.ButtonStyleKey}}">
                                <Setter Property="Background" Value="Transparent"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsStreaming}" Value="True">
                                        <Setter Property="Background" Value="{StaticResource SuccessSubtle}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <Grid>
                            <materialDesign:PackIcon Kind="AccessPoint" Width="24" Height="24"
                                                     Foreground="{StaticResource Success}"/>
                            <!-- Pulsing dot when streaming -->
                            <Ellipse Width="8" Height="8" Fill="{StaticResource Success}"
                                     HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-2,-2,0"
                                     Visibility="{Binding IsStreaming, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsStreaming}" Value="True">
                                                <DataTrigger.EnterActions>
                                                    <BeginStoryboard>
                                                        <Storyboard RepeatBehavior="Forever">
                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                             From="1" To="0.2" Duration="0:0:0.6"
                                                                             AutoReverse="True"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </DataTrigger.EnterActions>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                        </Grid>
                    </Button>

                    <!-- Separator -->
                    <Border Height="1" Width="32" Background="{StaticResource BorderSubtle}" Margin="0,6,0,6"/>
                    
                    <!-- Manage Logs -->
                    <Button Width="44" Height="44"
                            Background="Transparent" BorderThickness="0"
                            Margin="0,0,0,6" Cursor="Hand"
                            Command="{Binding ManageDebugLogsCommand}"
                            ToolTip="Manage Trace Flags &amp; Debug Logs"
                            Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10"/>
                            </Style>
                        </Button.Resources>
                        <materialDesign:PackIcon Kind="FileDocument" Foreground="{StaticResource TextSecondary}" Width="24" Height="24"/>
                    </Button>
                    
                    <!-- Export Report -->
                    <Button Width="44" Height="44"
                            Background="Transparent" BorderThickness="0"
                            Margin="0,0,0,6" Cursor="Hand"
                            Command="{Binding ExportReportCommand}"
                            ToolTip="Export analysis report"
                            Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10"/>
                            </Style>
                        </Button.Resources>
                        <materialDesign:PackIcon Kind="FileExport" Foreground="{StaticResource TextSecondary}" Width="24" Height="24"/>
                    </Button>
                </StackPanel>
                
                <!-- Bottom Actions -->
                <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,12">
                    <!-- Settings -->
                    <Button Width="44" Height="44"
                            Background="Transparent" BorderThickness="0"
                            Margin="0,0,0,6" Cursor="Hand"
                            Command="{Binding OpenSettingsCommand}"
                            ToolTip="Settings"
                            Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10"/>
                            </Style>
                        </Button.Resources>
                        <materialDesign:PackIcon Kind="Cog" Foreground="{StaticResource TextSecondary}" Width="24" Height="24"/>
                    </Button>
                    
                    <!-- Disconnect -->
                    <Button Width="44" Height="44"
                            Background="Transparent" BorderThickness="0"
                            Margin="0,0,0,0" Cursor="Hand"
                            Command="{Binding DisconnectCommand}"
                            ToolTip="Sign Out"
                            Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="10"/>
                            </Style>
                        </Button.Resources>
                        <materialDesign:PackIcon Kind="Power" Foreground="{StaticResource Danger}" Width="24" Height="24"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Row="1" Grid.Column="1" Background="Transparent" AllowDrop="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>  <!-- Trial expiry banner -->
                <RowDefinition Height="*"/>     <!-- Main content -->
            </Grid.RowDefinitions>

            <!-- Trial Expiry Banner -->
            <Border Grid.Row="0"
                    Visibility="{Binding ShowTrialBanner, Converter={StaticResource BooleanToVisibilityConverter}}"
                    Background="#F0B232" Padding="12,7">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{Binding TrialBannerText}"
                               FontSize="13" FontWeight="SemiBold"
                               Foreground="#1A1A1A" VerticalAlignment="Center"/>
                    <Button Grid.Column="1" Content="Upgrade Now →"
                            Command="{Binding UpgradeCommand}"
                            FontSize="12" FontWeight="Bold"
                            Background="#1A1A1A" Foreground="White"
                            BorderThickness="0" Padding="12,4" Cursor="Hand"
                            Margin="12,0,0,0"/>
                </Grid>
            </Border>

            <!-- Welcome/Connection View -->
            <ContentControl Grid.Row="1" x:Name="ConnectionsViewContainer" Visibility="Visible"/>

            <!-- Main Dashboard View (when connected) -->
            <Grid Grid.Row="1" x:Name="MainContentGrid" Visibility="Collapsed" Background="Transparent" AllowDrop="True">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="360" MinWidth="300" MaxWidth="450"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Panel - Logs & Filters -->
                <Border Grid.Column="0" CornerRadius="10" BorderBrush="{StaticResource BorderDefault}" BorderThickness="1"
                        Background="{StaticResource BgSecondary}">
                    <Grid Margin="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>  <!-- 0: Header -->
                            <RowDefinition Height="Auto"/>  <!-- 1: Search -->
                            <RowDefinition Height="*"/>     <!-- 2: Log List -->
                            <RowDefinition Height="Auto"/>  <!-- 3: Live Streaming -->
                        </Grid.RowDefinitions>

                        <!-- Header with action buttons -->
                        <Grid Grid.Row="0" Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Debug Logs" FontSize="18" FontWeight="Bold"
                                           VerticalAlignment="Center"
                                           Foreground="{StaticResource TextPrimary}"/>
                                <!-- Transaction grouping toggle -->
                                <Border CornerRadius="4" Padding="6,2" Margin="12,0,0,0" VerticalAlignment="Center"
                                        Background="{StaticResource AccentSubtle}"
                                        Visibility="{Binding LogGroups.Count, Converter={StaticResource GreaterThanZeroToVisibilityConverter}}"
                                        ToolTip="Transaction grouping enabled — logs from the same user action are grouped together">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="📦" FontSize="11" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                        <TextBlock FontSize="10" FontWeight="Bold" Foreground="{StaticResource AccentPrimary}" VerticalAlignment="Center">
                                            <Run Text="{Binding LogGroups.Count, Mode=OneWay}"/><Run Text=" groups"/>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                <!-- Group/Ungroup toggle -->
                                <ToggleButton IsChecked="{Binding ShowGroupedView}" Width="32" Height="32" Margin="0,0,8,0"
                                              Background="{StaticResource BgHover}" BorderThickness="0"
                                              Visibility="{Binding LogGroups.Count, Converter={StaticResource GreaterThanZeroToVisibilityConverter}}">
                                    <ToggleButton.Style>
                                        <Style TargetType="ToggleButton">
                                            <Setter Property="ToolTip" Value="Grouped View: use when you have multiple logs from the same transaction (e.g., a trigger + flow + future method all from one button click)"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Setter Property="ToolTip" Value="Switch back to individual log list"/>
                                                    <Setter Property="Background" Value="{StaticResource AccentPrimary}"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ToggleButton.Style>
                                    <materialDesign:PackIcon Width="16" Height="16" Foreground="White">
                                        <materialDesign:PackIcon.Style>
                                            <Style TargetType="materialDesign:PackIcon">
                                                <Setter Property="Kind" Value="ViewList"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ShowGroupedView}" Value="True">
                                                        <Setter Property="Kind" Value="ViewModule"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </materialDesign:PackIcon.Style>
                                    </materialDesign:PackIcon>
                                </ToggleButton>
                                <Button ToolTip="Open single log file" 
                                        Command="{Binding UploadLogCommand}"
                                        Width="32" Height="32" Padding="0" Margin="0,0,4,0"
                                        Background="{StaticResource BgHover}" BorderThickness="0">
                                    <materialDesign:PackIcon Kind="FileDocument" Width="16" Height="16" Foreground="White"/>
                                </Button>
                                <Button ToolTip="Load from folder" 
                                        Command="{Binding LoadLogFolderCommand}"
                                        Width="32" Height="32" Padding="0" Margin="0,0,4,0"
                                        Background="{StaticResource BgHover}" BorderThickness="0">
                                    <materialDesign:PackIcon Kind="FolderOpen" Width="16" Height="16" Foreground="White"/>
                                </Button>
                                <Button ToolTip="Download from org" 
                                        Command="{Binding LoadRecentLogsCommand}"
                                        Width="32" Height="32" Padding="0"
                                        Background="{StaticResource BgHover}" BorderThickness="0">
                                    <materialDesign:PackIcon Kind="CloudDownload" Width="16" Height="16" Foreground="White"/>
                                </Button>
                            </StackPanel>
                        </Grid>
                        
                        <!-- Search Box -->
                        <Border Background="{StaticResource BgInput}" CornerRadius="6" Grid.Row="1" Margin="0,0,0,12">
                            <Grid>
                                <TextBox x:Name="SearchBox" Background="Transparent" BorderThickness="0" 
                                         Foreground="{StaticResource TextPrimary}" Padding="10,8"
                                         FontSize="13"
                                         TextChanged="SearchBox_TextChanged"/>
                                <TextBlock Text="🔍 Search logs..." 
                                           Foreground="{StaticResource TextMuted}" Padding="10,8" FontSize="13"
                                           IsHitTestVisible="False">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </Border>
                        
                        <!-- Log List - Virtualized ListBox for performance -->
                        <ListBox Grid.Row="2" ItemsSource="{Binding Logs}" 
                                 SelectedItem="{Binding SelectedLog}"
                                 Background="Transparent" BorderThickness="0" Margin="0,0,0,8"
                                 VirtualizingStackPanel.IsVirtualizing="True"
                                 VirtualizingStackPanel.VirtualizationMode="Recycling"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 HorizontalContentAlignment="Stretch"
                                 Visibility="{Binding ShowGroupedView, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Margin" Value="0"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <!-- Pass ViewModel to ContextMenu via Tag (PlacementTarget pattern) -->
                                    <Setter Property="Tag" Value="{Binding DataContext, RelativeSource={RelativeSource AncestorType=ListBox}}"/>
                                    <Setter Property="ContextMenu">
                                        <Setter.Value>
                                            <ContextMenu DataContext="{Binding PlacementTarget.Tag, RelativeSource={RelativeSource Self}}">
                                                <MenuItem Header="🗑️ Remove from list" Command="{Binding DeleteSelectedLogCommand}"/>
                                                <MenuItem Header="📋 Copy file path"    Command="{Binding CopyLogPathCommand}"/>
                                                <MenuItem Header="✂️ Copy log name"     Command="{Binding CopyLogNameCommand}"/>
                                                <MenuItem Header="📁 Show in Explorer"  Command="{Binding OpenLogInExplorerCommand}"/>
                                                <Separator/>
                                                <MenuItem Header="✖ Deselect"           Command="{Binding ClearSelectedLogCommand}"/>
                                            </ContextMenu>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border x:Name="ItemBorder" CornerRadius="8" 
                                                        Margin="0,0,0,6" Cursor="Hand"
                                                        Background="{StaticResource BgSecondary}"
                                                        BorderBrush="{StaticResource BorderSubtle}" BorderThickness="1"
                                                        ClipToBounds="True">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="4"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <!-- Left accent bar — color-coded by status: red=error, yellow=warning, green=success -->
                                                        <Border x:Name="AccentBar" CornerRadius="7,0,0,7" Opacity="0.7">
                                                            <Border.Style>
                                                                <Style TargetType="Border">
                                                                    <Setter Property="Background" Value="{StaticResource Success}"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding TransactionFailed}" Value="True">
                                                                            <Setter Property="Background" Value="{StaticResource Danger}"/>
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding HasErrors}" Value="True">
                                                                            <Setter Property="Background" Value="{StaticResource Danger}"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Border.Style>
                                                        </Border>
                                                        <!-- Main content -->
                                                        <Grid Grid.Column="1" Margin="10,8,10,8">
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition Height="Auto"/>
                                                            </Grid.RowDefinitions>
                                                            
                                                            <!-- Row 0: Operation type icon + entry point name (what actually ran) -->
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Column="0" Text="{Binding OperationIcon}" FontSize="14"
                                                                           VerticalAlignment="Center" Margin="0,0,6,0"/>
                                                                <TextBlock Grid.Column="1" Text="{Binding OperationType, Mode=OneWay}" FontSize="12"
                                                                           FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}"
                                                                           TextTrimming="CharacterEllipsis" VerticalAlignment="Center"
                                                                           ToolTip="{Binding EntryPoint}"/>
                                                                <!-- Health score mini badge -->
                                                                <Border Grid.Column="2" CornerRadius="8" Padding="5,1" Margin="4,0,0,0"
                                                                        VerticalAlignment="Center"
                                                                        Visibility="{Binding Health, Converter={StaticResource NullToVisibilityConverter}}">
                                                                    <Border.Background>
                                                                        <SolidColorBrush Color="#22FFFFFF"/>
                                                                    </Border.Background>
                                                                    <TextBlock FontSize="10" FontWeight="Bold"
                                                                               Text="{Binding Health.Score, StringFormat={}{0}}"
                                                                               Foreground="{Binding Health.Score, Converter={StaticResource HealthScoreToColorConverter}}"/>
                                                                </Border>
                                                            </Grid>
                                                            
                                                            <!-- Row 1: Entry point / trigger name (the specific thing) -->
                                                            <TextBlock Grid.Row="1" Text="{Binding EntryPoint}" FontSize="11"
                                                                       Foreground="{StaticResource TextSecondary}" Margin="0,2,0,0"
                                                                       TextTrimming="CharacterEllipsis"
                                                                       ToolTip="{Binding EntryPoint}"/>
                                                            
                                                            <!-- Row 2: Duration + lines + log ID -->
                                                            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,4,0,0">
                                                                <!-- Duration with color coding -->
                                                                <TextBlock FontSize="11" FontWeight="SemiBold" Margin="0,0,0,0"
                                                                           Text="{Binding DurationMs, StringFormat={}{0:N0}ms}">
                                                                    <TextBlock.Style>
                                                                        <Style TargetType="TextBlock">
                                                                            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding DurationMs, Converter={StaticResource DoubleGreaterThanConverter}, ConverterParameter=3000}" Value="True">
                                                                                    <Setter Property="Foreground" Value="{StaticResource Danger}"/>
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </TextBlock.Style>
                                                                </TextBlock>
                                                                <TextBlock Text=" · " Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                                                <TextBlock FontSize="11" Foreground="{StaticResource TextMuted}"
                                                                           Text="{Binding LineCount, StringFormat={}{0:N0} lines}"/>
                                                                <TextBlock Text=" · " Foreground="{StaticResource TextMuted}" FontSize="11"/>
                                                                <TextBlock FontSize="10" Foreground="{StaticResource TextMuted}"
                                                                           Text="{Binding LogName}"
                                                                           TextTrimming="CharacterEllipsis"
                                                                           ToolTip="{Binding LogName}"/>
                                                            </StackPanel>
                                                            
                                                            <!-- Row 3: Badges row — status + issues + async + bulk -->
                                                            <WrapPanel Grid.Row="3" Orientation="Horizontal" Margin="0,5,0,0">
                                                                <!-- FAILED badge (only for errors — skip SUCCESS to reduce noise) -->
                                                                <Border CornerRadius="3" Padding="6,2" Margin="0,0,4,2"
                                                                        Visibility="{Binding HasErrors, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                                    <Border.Background>
                                                                        <SolidColorBrush Color="#F85149"/>
                                                                    </Border.Background>
                                                                    <TextBlock FontSize="9" FontWeight="Bold" Foreground="White">
                                                                        <TextBlock.Style>
                                                                            <Style TargetType="TextBlock">
                                                                                <Setter Property="Text" Value="ERROR"/>
                                                                                <Style.Triggers>
                                                                                    <DataTrigger Binding="{Binding TransactionFailed}" Value="True">
                                                                                        <Setter Property="Text" Value="FAILED"/>
                                                                                    </DataTrigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </TextBlock.Style>
                                                                    </TextBlock>
                                                                </Border>
                                                                <!-- Critical issues count badge -->
                                                                <Border CornerRadius="3" Padding="6,2" Margin="0,0,4,2"
                                                                        Background="{StaticResource DangerSubtle}"
                                                                        Visibility="{Binding Health.CriticalIssues.Count, Converter={StaticResource GreaterThanZeroToVisibilityConverter}, FallbackValue=Collapsed}">
                                                                    <TextBlock FontSize="9" FontWeight="Bold" Foreground="{StaticResource Danger}">
                                                                        <Run Text="{Binding Health.CriticalIssues.Count, Mode=OneWay}"/>
                                                                        <Run Text="CRITICAL"/>
                                                                    </TextBlock>
                                                                </Border>
                                                                <!-- Truncated log warning -->
                                                                <Border CornerRadius="3" Padding="5,2" Margin="0,0,4,2"
                                                                        Background="{StaticResource WarningSubtle}"
                                                                        Visibility="{Binding IsLogTruncated, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                                        ToolTip="Log was truncated by Salesforce — some data may be missing">
                                                                    <TextBlock Text="TRUNCATED" FontSize="9" FontWeight="Bold" Foreground="{StaticResource Warning}"/>
                                                                </Border>
                                                                <!-- ASYNC badge -->
                                                                <Border CornerRadius="3" Padding="6,2" Margin="0,0,4,2"
                                                                        Background="{StaticResource InfoSubtle}"
                                                                        Visibility="{Binding IsAsyncExecution, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                                    <TextBlock Text="ASYNC" FontSize="9" FontWeight="Bold" Foreground="{StaticResource Info}"/>
                                                                </Border>
                                                                <!-- Bulk Safety Grade pill -->
                                                                <Border CornerRadius="3" Padding="6,2" Margin="0,0,4,2"
                                                                        Visibility="{Binding BulkSafetyGrade, Converter={StaticResource StringToVisibilityConverter}}"
                                                                        ToolTip="{Binding BulkSafetyReason}">
                                                                    <Border.Style>
                                                                        <Style TargetType="Border">
                                                                            <Setter Property="Background" Value="{StaticResource SuccessSubtle}"/>
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding BulkSafetyGrade}" Value="C">
                                                                                    <Setter Property="Background" Value="{StaticResource WarningSubtle}"/>
                                                                                </DataTrigger>
                                                                                <DataTrigger Binding="{Binding BulkSafetyGrade}" Value="D">
                                                                                    <Setter Property="Background" Value="{StaticResource DangerSubtle}"/>
                                                                                </DataTrigger>
                                                                                <DataTrigger Binding="{Binding BulkSafetyGrade}" Value="F">
                                                                                    <Setter Property="Background" Value="{StaticResource DangerSubtle}"/>
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </Border.Style>
                                                                    <TextBlock FontSize="9" FontWeight="Bold">
                                                                        <Run Text="Bulk "/>
                                                                        <Run Text="{Binding BulkSafetyGrade}"/>
                                                                        <TextBlock.Style>
                                                                            <Style TargetType="TextBlock">
                                                                                <Setter Property="Foreground" Value="{StaticResource Success}"/>
                                                                                <Style.Triggers>
                                                                                    <DataTrigger Binding="{Binding BulkSafetyGrade}" Value="C">
                                                                                        <Setter Property="Foreground" Value="{StaticResource Warning}"/>
                                                                                    </DataTrigger>
                                                                                    <DataTrigger Binding="{Binding BulkSafetyGrade}" Value="D">
                                                                                        <Setter Property="Foreground" Value="{StaticResource Danger}"/>
                                                                                    </DataTrigger>
                                                                                    <DataTrigger Binding="{Binding BulkSafetyGrade}" Value="F">
                                                                                        <Setter Property="Foreground" Value="{StaticResource Danger}"/>
                                                                                    </DataTrigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </TextBlock.Style>
                                                                    </TextBlock>
                                                                </Border>
                                                            </WrapPanel>
                                                        </Grid>
                                                    </Grid>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource BgHover}"/>
                                                        <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{StaticResource AccentPrimary}"/>
                                                        <Setter TargetName="AccentBar" Property="Opacity" Value="1"/>
                                                        <Setter TargetName="ItemBorder" Property="Effect">
                                                            <Setter.Value>
                                                                <DropShadowEffect BlurRadius="14" Color="#4493F8" Opacity="0.3" ShadowDepth="0"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                    <MultiTrigger>
                                                        <MultiTrigger.Conditions>
                                                            <Condition Property="IsMouseOver" Value="True"/>
                                                            <Condition Property="IsSelected" Value="False"/>
                                                        </MultiTrigger.Conditions>
                                                        <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource BgHover}"/>
                                                        <Setter TargetName="AccentBar" Property="Opacity" Value="0.9"/>
                                                    </MultiTrigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                        
                        <!-- Grouped View info banner (shown when ShowGroupedView = true) -->
                        <Border Grid.Row="2" VerticalAlignment="Top" Padding="10,8" Margin="0,0,0,4"
                                Background="{StaticResource BgInput}" CornerRadius="6"
                                Visibility="{Binding ShowGroupedView, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="{StaticResource TextSecondary}">
                                <Run FontWeight="SemiBold" Foreground="{StaticResource AccentPrimary}" Text="🔗 Grouped View — "/>
                                <Run Text="Shows logs from the same transaction together. Select a group to see the combined picture."/>
                            </TextBlock>
                        </Border>

                        <!-- Transaction Group List (shown when ShowGroupedView = true) -->
                        <ListBox Grid.Row="2" ItemsSource="{Binding LogGroups}"
                                 SelectedItem="{Binding SelectedLogGroup}"
                                 Background="Transparent" BorderThickness="0" Margin="0,44,0,8"
                                 VirtualizingStackPanel.IsVirtualizing="True"
                                 VirtualizingStackPanel.VirtualizationMode="Recycling"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 HorizontalContentAlignment="Stretch"
                                 Visibility="{Binding ShowGroupedView, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Margin" Value="0"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <Border x:Name="GroupBorder" CornerRadius="6" 
                                                        Padding="12" Margin="0,0,0,8" Cursor="Hand"
                                                        Background="{StaticResource BgInput}"
                                                        BorderBrush="{StaticResource BorderDefault}" BorderThickness="1">
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                            <RowDefinition Height="Auto"/>
                                                        </Grid.RowDefinitions>
                                                        
                                                        <!-- Row 0: Header with user + log count -->
                                                        <Grid Grid.Row="0">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto"/>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock Grid.Column="0" Text="📦" FontSize="16" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                                <TextBlock FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}"
                                                                           TextTrimming="CharacterEllipsis">
                                                                    <Run Text="{Binding UserName, Mode=OneWay}"/>
                                                                    <Run Text=" transaction" Foreground="{StaticResource TextMuted}"/>
                                                                </TextBlock>
                                                                <TextBlock FontSize="10" Foreground="{StaticResource TextMuted}" Margin="0,2,0,0">
                                                                    <Run Text="{Binding Logs.Count, Mode=OneWay}"/><Run Text=" logs • "/><Run Text="{Binding TotalDuration, StringFormat={}{0:N0}ms, Mode=OneWay}"/><Run Text=" total"/>
                                                                </TextBlock>
                                                            </StackPanel>
                                                            <Border Grid.Column="2" CornerRadius="3" Padding="6,2" Background="{StaticResource AccentSubtle}" VerticalAlignment="Center"
                                                                    Visibility="{Binding IsSingleLog, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                                                                <TextBlock Text="GROUPED" FontSize="9" FontWeight="Bold" Foreground="{StaticResource AccentPrimary}"/>
                                                            </Border>
                                                        </Grid>
                                                        
                                                        <!-- Row 1: Phase breakdown (when phases detected) -->
                                                        <StackPanel Grid.Row="1" Margin="22,6,0,0"
                                                                    Visibility="{Binding Phases.Count, Converter={StaticResource GreaterThanZeroToVisibilityConverter}}">
                                                            <ItemsControl ItemsSource="{Binding Phases}">
                                                                <ItemsControl.ItemTemplate>
                                                                    <DataTemplate>
                                                                        <Grid Margin="0,2">
                                                                            <Grid.ColumnDefinitions>
                                                                                <ColumnDefinition Width="Auto"/>
                                                                                <ColumnDefinition Width="*"/>
                                                                                <ColumnDefinition Width="Auto"/>
                                                                            </Grid.ColumnDefinitions>
                                                                            <Border Grid.Column="0" Width="4" Height="4" CornerRadius="2" Margin="0,0,6,0" VerticalAlignment="Center">
                                                                                <Border.Style>
                                                                                    <Style TargetType="Border">
                                                                                        <Setter Property="Background" Value="{StaticResource Info}"/>
                                                                                        <Style.Triggers>
                                                                                            <DataTrigger Binding="{Binding Type}" Value="Frontend">
                                                                                                <Setter Property="Background" Value="{StaticResource Success}"/>
                                                                                            </DataTrigger>
                                                                                        </Style.Triggers>
                                                                                    </Style>
                                                                                </Border.Style>
                                                                            </Border>
                                                                            <TextBlock Grid.Column="1" FontSize="10" Foreground="{StaticResource TextSecondary}">
                                                                                <Run Text="{Binding Name, Mode=OneWay}"/><Run Text=":"/>
                                                                            </TextBlock>
                                                                            <TextBlock Grid.Column="2" FontSize="10" Foreground="{StaticResource TextMuted}">
                                                                                <Run Text="{Binding DurationMs, StringFormat={}{0:N0}ms, Mode=OneWay}"/>
                                                                            </TextBlock>
                                                                        </Grid>
                                                                    </DataTemplate>
                                                                </ItemsControl.ItemTemplate>
                                                            </ItemsControl>
                                                        </StackPanel>
                                                        
                                                        <!-- Row 2: Recommendations count (when available) -->
                                                        <Border Grid.Row="2" CornerRadius="3" Padding="6,3" Margin="22,6,0,0" HorizontalAlignment="Left"
                                                                Background="{StaticResource WarningSubtle}"
                                                                Visibility="{Binding Recommendations.Count, Converter={StaticResource GreaterThanZeroToVisibilityConverter}}">
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="💡" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                                                <TextBlock FontSize="9" FontWeight="Bold" Foreground="{StaticResource Warning}" VerticalAlignment="Center">
                                                                    <Run Text="{Binding Recommendations.Count, Mode=OneWay}"/><Run Text=" recommendation"/>
                                                                </TextBlock>
                                                            </StackPanel>
                                                        </Border>
                                                        
                                                        <!-- Row 3: Mixed context warning -->
                                                        <Border Grid.Row="3" CornerRadius="3" Padding="6,3" Margin="22,6,0,0" HorizontalAlignment="Left"
                                                                Background="{StaticResource DangerSubtle}"
                                                                Visibility="{Binding HasMixedContexts, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                            <StackPanel Orientation="Horizontal">
                                                                <TextBlock Text="⚠️" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                                                <TextBlock Text="MIXED CONTEXTS" FontSize="9" FontWeight="Bold" Foreground="{StaticResource Danger}" VerticalAlignment="Center"/>
                                                            </StackPanel>
                                                        </Border>
                                                    </Grid>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsSelected" Value="True">
                                                        <Setter TargetName="GroupBorder" Property="Background" Value="{StaticResource AccentPrimary}"/>
                                                        <Setter TargetName="GroupBorder" Property="BorderBrush" Value="{StaticResource AccentPrimary}"/>
                                                    </Trigger>
                                                    <MultiTrigger>
                                                        <MultiTrigger.Conditions>
                                                            <Condition Property="IsMouseOver" Value="True"/>
                                                            <Condition Property="IsSelected" Value="False"/>
                                                        </MultiTrigger.Conditions>
                                                        <Setter TargetName="GroupBorder" Property="Background" Value="{StaticResource BgHover}"/>
                                                    </MultiTrigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                        
                        <!-- Empty state when no logs loaded -->
                        <StackPanel Grid.Row="2" Margin="12,20,12,8"
                                    Visibility="{Binding Logs.Count, Converter={StaticResource ZeroToVisibilityConverter}}"
                                    HorizontalAlignment="Center">
                            <materialDesign:PackIcon Kind="FileDocumentOutline" Width="28" Height="28"
                                                     Foreground="{StaticResource TextDisabled}"
                                                     HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock Text="No logs loaded" FontSize="12" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                            <TextBlock Text="Drop .log files here or&#x0a;connect to Salesforce"
                                       FontSize="11" Foreground="{StaticResource TextMuted}" TextWrapping="Wrap"
                                       TextAlignment="Center" HorizontalAlignment="Center" LineHeight="17"/>
                        </StackPanel>

                        <Border Background="{StaticResource BgInput}" CornerRadius="6" 
                                Grid.Row="3" Padding="8,6" Margin="0,0,0,8"
                                Visibility="{Binding IsStreaming, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <WrapPanel>
                                    <CheckBox Content="Reads (SOQL)" IsChecked="{Binding ShowSoqlOperations}" Foreground="{StaticResource TextSecondary}" Margin="0,2,12,2" FontSize="11" ToolTip="Database read operations (SOQL queries)"/>
                                    <CheckBox Content="Writes (DML)" IsChecked="{Binding ShowDmlOperations}" Foreground="{StaticResource TextSecondary}" Margin="0,2,12,2" FontSize="11" ToolTip="Database write operations (inserts, updates, deletes)"/>
                                    <CheckBox Content="Errors" IsChecked="{Binding ShowErrorsOnly}" Foreground="{StaticResource TextSecondary}" Margin="0,2,12,2" FontSize="11"/>
                                    <CheckBox Content="Debug" IsChecked="{Binding ShowDebugStatements}" Foreground="{StaticResource TextSecondary}" Margin="0,2,12,2" FontSize="11"/>
                                </WrapPanel>
                                <!-- Sort buttons (right side) -->
                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0,0,0">
                                    <TextBlock Text="Sort:" FontSize="10" Foreground="{StaticResource TextMuted}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                    <Button Command="{Binding SortLogsCommand}" CommandParameter="Date"
                                            ToolTip="Sort by date (newest first)" Cursor="Hand"
                                            Background="Transparent" BorderThickness="0" Padding="4,3">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="CalendarToday" Width="11" Height="11" VerticalAlignment="Center">
                                                <materialDesign:PackIcon.Style>
                                                    <Style TargetType="materialDesign:PackIcon">
                                                        <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding LogSortProperty}" Value="Date">
                                                                <Setter Property="Foreground" Value="{StaticResource AccentPrimary}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </materialDesign:PackIcon.Style>
                                            </materialDesign:PackIcon>
                                            <TextBlock Text="Date" FontSize="10" Margin="3,0,0,0">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding LogSortProperty}" Value="Date">
                                                                <Setter Property="Foreground" Value="{StaticResource AccentPrimary}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                    </Button>
                                    <Button Command="{Binding SortLogsCommand}" CommandParameter="Duration"
                                            ToolTip="Sort by execution time (slowest first)" Cursor="Hand"
                                            Background="Transparent" BorderThickness="0" Padding="4,3">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="Timer" Width="11" Height="11" VerticalAlignment="Center">
                                                <materialDesign:PackIcon.Style>
                                                    <Style TargetType="materialDesign:PackIcon">
                                                        <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding LogSortProperty}" Value="Duration">
                                                                <Setter Property="Foreground" Value="{StaticResource Warning}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </materialDesign:PackIcon.Style>
                                            </materialDesign:PackIcon>
                                            <TextBlock Text="Speed" FontSize="10" Margin="3,0,0,0">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding LogSortProperty}" Value="Duration">
                                                                <Setter Property="Foreground" Value="{StaticResource Warning}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                    </Button>
                                    <Button Command="{Binding SortLogsCommand}" CommandParameter="Status"
                                            ToolTip="Sort by status (errors first)" Cursor="Hand"
                                            Background="Transparent" BorderThickness="0" Padding="4,3">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="AlertCircle" Width="11" Height="11" VerticalAlignment="Center">
                                                <materialDesign:PackIcon.Style>
                                                    <Style TargetType="materialDesign:PackIcon">
                                                        <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding LogSortProperty}" Value="Status">
                                                                <Setter Property="Foreground" Value="{StaticResource Danger}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </materialDesign:PackIcon.Style>
                                            </materialDesign:PackIcon>
                                            <TextBlock Text="Errors" FontSize="10" Margin="3,0,0,0">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding LogSortProperty}" Value="Status">
                                                                <Setter Property="Foreground" Value="{StaticResource Danger}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                        
                        <!-- Live Streaming Panel (shown when streaming) -->
                        <Border Background="{StaticResource BgTertiary}" CornerRadius="8" 
                                Grid.Row="3" Padding="12" Margin="0,8,0,0"
                                Visibility="{Binding IsStreaming, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <!-- Header -->
                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                                    <Ellipse Width="10" Height="10" Fill="{StaticResource Danger}" Margin="0,0,8,0">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsStreaming}" Value="True">
                                                        <DataTrigger.EnterActions>
                                                            <BeginStoryboard>
                                                                <Storyboard RepeatBehavior="Forever">
                                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                                     From="1" To="0.3" Duration="0:0:0.5"
                                                                                     AutoReverse="True"/>
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </DataTrigger.EnterActions>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                    <TextBlock Text="📡 Live Stream" FontSize="14" FontWeight="SemiBold" 
                                               Foreground="{StaticResource Danger}"/>
                                </StackPanel>
                                
                                <!-- Stream Output -->
                                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="200">
                                    <ItemsControl ItemsSource="{Binding StreamingLogs}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Margin="0,4" CornerRadius="4" Cursor="Hand">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="Background" Value="{StaticResource BgTertiary}"/>
                                                            <Setter Property="Padding" Value="8"/>
                                                            <Style.Triggers>
                                                                <!-- Log entries with Analysis (actual logs) -->
                                                                <DataTrigger Binding="{Binding Analysis, Converter={StaticResource NullToVisibilityConverter}}" Value="Visible">
                                                                    <Setter Property="Background" Value="{StaticResource BgHover}"/>
                                                                </DataTrigger>
                                                                <!-- Status messages (no Analysis) -->
                                                                <DataTrigger Binding="{Binding Analysis}" Value="{x:Null}">
                                                                    <Setter Property="Padding" Value="4,2"/>
                                                                    <Setter Property="Background" Value="Transparent"/>
                                                                </DataTrigger>
                                                                <!-- Error messages -->
                                                                <DataTrigger Binding="{Binding IsError}" Value="True">
                                                                    <Setter Property="Background" Value="{StaticResource DangerSubtle}"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    
                                                    <Grid>
                                                        <!-- Rich Log Card (when Analysis exists) -->
                                                        <Grid Visibility="{Binding Analysis, Converter={StaticResource NullToVisibilityConverter}}">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            
                                                            <StackPanel>
                                                                <!-- Log ID and Timestamp -->
                                                                <Grid Margin="0,0,0,4">
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="Auto"/>
                                                                        <ColumnDefinition Width="*"/>
                                                                    </Grid.ColumnDefinitions>
                                                                    <TextBlock Text="{Binding LogId}" FontWeight="SemiBold" 
                                                                               FontSize="11" Foreground="White"/>
                                                                    <TextBlock Grid.Column="1" FontSize="9" Foreground="{StaticResource TextMuted}" 
                                                                               HorizontalAlignment="Right" VerticalAlignment="Center">
                                                                        <Run Text="{Binding Timestamp, StringFormat=h:mm:ss tt}"/>
                                                                    </TextBlock>
                                                                </Grid>
                                                                
                                                                <!-- Operation Type and User -->
                                                                <TextBlock FontSize="10" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,2">
                                                                    <Run Text="{Binding OperationType, Mode=OneWay}" FontWeight="SemiBold"/>
                                                                    <Run Text=" by " Foreground="{StaticResource TextMuted}"/>
                                                                    <Run Text="{Binding User}" Foreground="{StaticResource AccentPrimary}"/>
                                                                </TextBlock>
                                                                
                                                                <!-- Plain English Insight (NEW!) -->
                                                                <TextBlock FontSize="10" Foreground="White" Margin="0,4,0,2" 
                                                                           FontWeight="SemiBold" TextWrapping="Wrap">
                                                                    <Run Text="{Binding InsightIcon}"/>
                                                                    <Run Text=" "/>
                                                                    <Run Text="{Binding Insight}"/>
                                                                </TextBlock>
                                                                
                                                                <!-- Duration and Lines -->
                                                                <TextBlock FontSize="9" Foreground="{StaticResource TextMuted}">
                                                                    <Run Text="{Binding Duration}"/>
                                                                    <Run Text=" � "/>
                                                                    <Run Text="{Binding LineCount}"/>
                                                                    <Run Text=" lines"/>
                                                                </TextBlock>
                                                            </StackPanel>
                                                            
                                                            <!-- Status Badge -->
                                                            <Border Grid.Column="1" CornerRadius="3" Padding="6,2" 
                                                                    VerticalAlignment="Top" Margin="8,0,0,0">
                                                                <Border.Style>
                                                                    <Style TargetType="Border">
                                                                        <Setter Property="Background" Value="{StaticResource Success}"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding IsError}" Value="True">
                                                                                <Setter Property="Background" Value="{StaticResource Danger}"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </Border.Style>
                                                                <TextBlock Text="{Binding Status}" FontSize="9" 
                                                                           FontWeight="Bold" Foreground="White"/>
                                                            </Border>
                                                        </Grid>
                                                        
                                                        <!-- Simple Status Message (no Analysis) -->
                                                        <StackPanel Visibility="{Binding Analysis, Converter={StaticResource InverseNullToVisibilityConverter}}">
                                                            <TextBlock FontSize="10" Foreground="{StaticResource TextMuted}">
                                                                <Run Text="["/><Run Text="{Binding Timestamp, StringFormat=HH:mm:ss}"/><Run Text="]"/>
                                                            </TextBlock>
                                                            <TextBlock Text="{Binding Message}" TextWrapping="Wrap" FontSize="11">
                                                                <TextBlock.Style>
                                                                    <Style TargetType="TextBlock">
                                                                        <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                                                                        <Style.Triggers>
                                                                            <DataTrigger Binding="{Binding IsError}" Value="True">
                                                                                <Setter Property="Foreground" Value="{StaticResource Danger}"/>
                                                                            </DataTrigger>
                                                                        </Style.Triggers>
                                                                    </Style>
                                                                </TextBlock.Style>
                                                            </TextBlock>
                                                        </StackPanel>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                                
                                <!-- Recording/Stop Buttons -->
                                <Grid Grid.Row="2" Margin="0,12,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="8"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Start Recording Button (when not recording) -->
                                    <Button Grid.Column="0">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="RecordCircle" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                            <TextBlock Text="Record Interaction" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                    
                                    <!-- Stop Recording Button (when recording) -->
                                    <Button Grid.Column="0" 
                                            Command="{Binding StopRecordingAsyncCommand}"
                                            Style="{StaticResource DiscordPrimaryButton}"
                                            Background="{StaticResource Danger}"
                                            Visibility="{Binding IsRecording, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <StackPanel Orientation="Horizontal">
                                            <Ellipse Width="8" Height="8" Fill="White" Margin="0,0,8,0">
                                                <Ellipse.Style>
                                                    <Style TargetType="Ellipse">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsRecording}" Value="True">
                                                                <DataTrigger.EnterActions>
                                                                    <BeginStoryboard>
                                                                        <Storyboard RepeatBehavior="Forever">
                                                                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                                             From="1" To="0.3" Duration="0:0:0.5"
                                                                                             AutoReverse="True"/>
                                                                        </Storyboard>
                                                                    </BeginStoryboard>
                                                                </DataTrigger.EnterActions>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Ellipse.Style>
                                            </Ellipse>
                                            <TextBlock Text="? Stop Recording"/>
                                            <TextBlock Text="{Binding RecordingElapsedTime}" Margin="8,0,0,0" Opacity="0.8"/>
                                        </StackPanel>
                                    </Button>
                                    
                                    <!-- Stop Streaming Button -->
                                    <Button Grid.Column="2" Content="? Stop Streaming" 
                                            Command="{Binding ToggleStreamingCommand}"
                                            Style="{StaticResource DiscordSecondaryButton}"
                                            IsEnabled="{Binding IsRecording, Converter={StaticResource InverseBooleanConverter}}"/>
                                </Grid>
                                
                                <!-- Recorded Interactions List -->
                                <Border Grid.Row="3" Background="{StaticResource BgInput}" CornerRadius="6" 
                                        Margin="0,12,0,0" Padding="8"
                                        Visibility="{Binding Interactions.Count, Converter={StaticResource GreaterThanZeroToVisibilityConverter}}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Text="📼 Recorded Interactions" FontSize="12" FontWeight="SemiBold" 
                                                   Foreground="{StaticResource TextSecondary}" Margin="0,0,0,8"/>
                                        <ScrollViewer Grid.Row="1" MaxHeight="150" VerticalScrollBarVisibility="Auto">
                                            <ItemsControl ItemsSource="{Binding Interactions}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Background="{StaticResource BgSecondary}" CornerRadius="4" 
                                                                Padding="8" Margin="0,0,0,4" Cursor="Hand"
                                                                MouseLeftButtonUp="InteractionCard_Click">
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                <StackPanel>
                                                                    <TextBlock Text="{Binding Name}" FontSize="12" 
                                                                               FontWeight="SemiBold" Foreground="White"/>
                                                                    <TextBlock Text="{Binding Summary}" FontSize="10" 
                                                                               Foreground="{StaticResource TextMuted}" Margin="0,2,0,0"/>
                                                                </StackPanel>
                                                                <Border Grid.Column="1" CornerRadius="3" Padding="6,2" 
                                                                        VerticalAlignment="Center">
                                                                    <Border.Style>
                                                                        <Style TargetType="Border">
                                                                            <Setter Property="Background" Value="{StaticResource Success}"/>
                                                                            <Style.Triggers>
                                                                                <DataTrigger Binding="{Binding HasErrors}" Value="True">
                                                                                    <Setter Property="Background" Value="{StaticResource Danger}"/>
                                                                                </DataTrigger>
                                                                            </Style.Triggers>
                                                                        </Style>
                                                                    </Border.Style>
                                                                    <TextBlock FontSize="9" FontWeight="Bold" Foreground="White">
                                                                        <TextBlock.Style>
                                                                            <Style TargetType="TextBlock">
                                                                                <Setter Property="Text" Value="OK"/>
                                                                                <Style.Triggers>
                                                                                    <DataTrigger Binding="{Binding HasErrors}" Value="True">
                                                                                        <Setter Property="Text" Value="ERROR"/>
                                                                                    </DataTrigger>
                                                                                </Style.Triggers>
                                                                            </Style>
                                                                        </TextBlock.Style>
                                                                    </TextBlock>
                                                                </Border>
                                                            </Grid>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </Grid>
                                </Border>
                            </Grid>
                        </Border>
                    </Grid>
                </Border>

                <!-- Spacer -->
                <Grid Grid.Column="1"/>

                <!-- Right Content - Analysis with Tabs -->
                <Border Grid.Column="2" Style="{StaticResource DiscordCard}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <!-- Tab Headers — compact underline style — Order: Overview | Explain | Timeline | Queries | Tree | Security | Debug(hidden) -->
                        <Border Grid.Row="0" BorderBrush="{StaticResource BorderDefault}" BorderThickness="0,0,0,1" Padding="4,0"
                                Background="{StaticResource BgPrimary}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                            <StackPanel Orientation="Horizontal">
                                <!-- Overview Tab (index 0) — formerly Summary -->
                                <Button Command="{Binding SelectTabCommand}" Cursor="Hand" BorderThickness="0" Padding="14,0" Height="40">
                                    <Button.CommandParameter><sys:Int32>0</sys:Int32></Button.CommandParameter>
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Grid>
                                                <Border x:Name="Bg" Background="Transparent" Padding="{TemplateBinding Padding}">
                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                        <materialDesign:PackIcon Kind="ChartBox" Width="14" Height="14" VerticalAlignment="Center" Margin="0,0,5,0"
                                                                                 Foreground="{StaticResource TextMuted}"/>
                                                        <TextBlock Text="Overview" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding SelectedTabIndex}" Value="0">
                                                                            <Setter Property="Foreground" Value="White"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </StackPanel>
                                                </Border>
                                                <Border x:Name="Underline" Height="2" VerticalAlignment="Bottom" CornerRadius="1,1,0,0" Visibility="Collapsed">
                                                    <Border.Background><SolidColorBrush Color="#4493F8"/></Border.Background>
                                                </Border>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <DataTrigger Binding="{Binding SelectedTabIndex}" Value="0">
                                                    <Setter TargetName="Underline" Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bg" Property="Background" Value="{StaticResource BgHover}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                                <!-- Explain Tab (index 4) -->
                                <Button Command="{Binding SelectTabCommand}" Cursor="Hand" BorderThickness="0" Padding="14,0" Height="40">
                                    <Button.CommandParameter><sys:Int32>4</sys:Int32></Button.CommandParameter>
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Grid>
                                                <Border x:Name="Bg" Background="Transparent" Padding="{TemplateBinding Padding}">
                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                        <materialDesign:PackIcon Kind="Lightbulb" Width="14" Height="14" VerticalAlignment="Center" Margin="0,0,5,0"
                                                                                 Foreground="{StaticResource TextMuted}"/>
                                                        <TextBlock Text="Explain" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding SelectedTabIndex}" Value="4">
                                                                            <Setter Property="Foreground" Value="White"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </StackPanel>
                                                </Border>
                                                <Border x:Name="Underline" Height="2" VerticalAlignment="Bottom" CornerRadius="1,1,0,0" Visibility="Collapsed">
                                                    <Border.Background><SolidColorBrush Color="#4493F8"/></Border.Background>
                                                </Border>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <DataTrigger Binding="{Binding SelectedTabIndex}" Value="4">
                                                    <Setter TargetName="Underline" Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bg" Property="Background" Value="{StaticResource BgHover}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                                <!-- Timeline Tab (index 2) -->
                                <Button Command="{Binding SelectTabCommand}" Cursor="Hand" BorderThickness="0" Padding="14,0" Height="40">
                                    <Button.CommandParameter><sys:Int32>2</sys:Int32></Button.CommandParameter>
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Grid>
                                                <Border x:Name="Bg" Background="Transparent" Padding="{TemplateBinding Padding}">
                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                        <materialDesign:PackIcon Kind="Timeline" Width="14" Height="14" VerticalAlignment="Center" Margin="0,0,5,0"
                                                                                 Foreground="{StaticResource TextMuted}"/>
                                                        <TextBlock Text="Timeline" FontSize="13" VerticalAlignment="Center">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding SelectedTabIndex}" Value="2">
                                                                            <Setter Property="Foreground" Value="White"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </StackPanel>
                                                </Border>
                                                <Border x:Name="Underline" Height="2" VerticalAlignment="Bottom" CornerRadius="1,1,0,0" Visibility="Collapsed">
                                                    <Border.Background><SolidColorBrush Color="#4493F8"/></Border.Background>
                                                </Border>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <DataTrigger Binding="{Binding SelectedTabIndex}" Value="2">
                                                    <Setter TargetName="Underline" Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bg" Property="Background" Value="{StaticResource BgHover}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                                <!-- Queries Tab (index 3) -->
                                <Button Command="{Binding SelectTabCommand}" Cursor="Hand" BorderThickness="0" Padding="14,0" Height="40">
                                    <Button.CommandParameter><sys:Int32>3</sys:Int32></Button.CommandParameter>
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Grid>
                                                <Border x:Name="Bg" Background="Transparent" Padding="{TemplateBinding Padding}">
                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                        <materialDesign:PackIcon Kind="DatabaseSearch" Width="14" Height="14" VerticalAlignment="Center" Margin="0,0,5,0"
                                                                                 Foreground="{StaticResource TextMuted}"/>
                                                        <TextBlock Text="Queries" FontSize="13" VerticalAlignment="Center">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding SelectedTabIndex}" Value="3">
                                                                            <Setter Property="Foreground" Value="White"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </StackPanel>
                                                </Border>
                                                <Border x:Name="Underline" Height="2" VerticalAlignment="Bottom" CornerRadius="1,1,0,0" Visibility="Collapsed">
                                                    <Border.Background><SolidColorBrush Color="#4493F8"/></Border.Background>
                                                </Border>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <DataTrigger Binding="{Binding SelectedTabIndex}" Value="3">
                                                    <Setter TargetName="Underline" Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bg" Property="Background" Value="{StaticResource BgHover}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                                <!-- Tree Tab (index 1) -->
                                <Button Command="{Binding SelectTabCommand}" Cursor="Hand" BorderThickness="0" Padding="14,0" Height="40">
                                    <Button.CommandParameter><sys:Int32>1</sys:Int32></Button.CommandParameter>
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Grid>
                                                <Border x:Name="Bg" Background="Transparent" Padding="{TemplateBinding Padding}">
                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                        <materialDesign:PackIcon Kind="FileTree" Width="14" Height="14" VerticalAlignment="Center" Margin="0,0,5,0"
                                                                                 Foreground="{StaticResource TextMuted}"/>
                                                        <TextBlock Text="Tree" FontSize="13" VerticalAlignment="Center">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding SelectedTabIndex}" Value="1">
                                                                            <Setter Property="Foreground" Value="White"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </StackPanel>
                                                </Border>
                                                <Border x:Name="Underline" Height="2" VerticalAlignment="Bottom" CornerRadius="1,1,0,0" Visibility="Collapsed">
                                                    <Border.Background><SolidColorBrush Color="#4493F8"/></Border.Background>
                                                </Border>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <DataTrigger Binding="{Binding SelectedTabIndex}" Value="1">
                                                    <Setter TargetName="Underline" Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bg" Property="Background" Value="{StaticResource BgHover}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                                <!-- Security Tab (index 6) — merged Shield + PII -->
                                <Button Command="{Binding SelectTabCommand}" Cursor="Hand" BorderThickness="0" Padding="14,0" Height="40"
                                        Visibility="{Binding IsConnected, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <Button.CommandParameter><sys:Int32>6</sys:Int32></Button.CommandParameter>
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Grid>
                                                <Border x:Name="Bg" Background="Transparent" Padding="{TemplateBinding Padding}">
                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                        <materialDesign:PackIcon Kind="ShieldLock" Width="14" Height="14" VerticalAlignment="Center" Margin="0,0,5,0"
                                                                                 Foreground="{StaticResource TextMuted}"/>
                                                        <TextBlock Text="Security" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding SelectedTabIndex}" Value="6">
                                                                            <Setter Property="Foreground" Value="White"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </StackPanel>
                                                </Border>
                                                <Border x:Name="Underline" Height="2" VerticalAlignment="Bottom" CornerRadius="1,1,0,0" Visibility="Collapsed">
                                                    <Border.Background><SolidColorBrush Color="#4493F8"/></Border.Background>
                                                </Border>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <DataTrigger Binding="{Binding SelectedTabIndex}" Value="6">
                                                    <Setter TargetName="Underline" Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bg" Property="Background" Value="{StaticResource BgHover}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                                <!-- Debug Tab (index 5) — hidden unless debug mode is on -->
                                <Button Command="{Binding SelectTabCommand}" Cursor="Hand" BorderThickness="0" Padding="14,0" Height="40"
                                        Visibility="{Binding IsParserDebugMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <Button.CommandParameter><sys:Int32>5</sys:Int32></Button.CommandParameter>
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Grid>
                                                <Border x:Name="Bg" Background="Transparent" Padding="{TemplateBinding Padding}">
                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                        <materialDesign:PackIcon Kind="Bug" Width="14" Height="14" VerticalAlignment="Center" Margin="0,0,5,0"
                                                                                 Foreground="{StaticResource TextMuted}"/>
                                                        <TextBlock Text="Debug" FontSize="13" VerticalAlignment="Center">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding SelectedTabIndex}" Value="5">
                                                                            <Setter Property="Foreground" Value="White"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </StackPanel>
                                                </Border>
                                                <Border x:Name="Underline" Height="2" VerticalAlignment="Bottom" CornerRadius="1,1,0,0" Visibility="Collapsed">
                                                    <Border.Background><SolidColorBrush Color="#4493F8"/></Border.Background>
                                                </Border>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <DataTrigger Binding="{Binding SelectedTabIndex}" Value="5">
                                                    <Setter TargetName="Underline" Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bg" Property="Background" Value="{StaticResource BgHover}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </StackPanel>
                            </ScrollViewer>
                            <!-- Right side: Search + Export in tab bar -->
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                                <Button Command="{Binding ExportReportCommand}" Cursor="Hand"
                                        Background="Transparent" BorderThickness="0" Padding="6,4"
                                        ToolTip="Export report">
                                    <materialDesign:PackIcon Kind="Download" Width="15" Height="15" Foreground="{StaticResource TextMuted}"/>
                                </Button>
                            </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Tab Content -->
                        <Grid Grid.Row="1">
                            <!-- "No log selected" empty state — full welcome screen -->
                            <Border Background="{StaticResource BgPrimary}" 
                                    Visibility="{Binding SelectedLog, Converter={StaticResource InverseNullToVisibilityConverter}}"
                                    Panel.ZIndex="10">
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                                                MaxWidth="560" Margin="40,0">
                                        <!-- Logo / Hero -->
                                        <materialDesign:PackIcon Kind="Spider" Width="48" Height="48"
                                                             Foreground="{StaticResource AccentPrimary}"
                                                             HorizontalAlignment="Center" Margin="0,40,0,0"/>
                                        <TextBlock Text="Black Widow" FontSize="28" FontWeight="Bold"
                                                   Foreground="{StaticResource TextPrimary}"
                                                   HorizontalAlignment="Center" Margin="0,10,0,4"/>
                                        <TextBlock Text="Salesforce Debug Log Analyzer"
                                                   FontSize="14" Foreground="{StaticResource TextMuted}"
                                                   HorizontalAlignment="Center" Margin="0,0,0,36"/>

                                        <!-- How do you want to start? -->
                                        <TextBlock Text="How do you want to start?" FontSize="15" FontWeight="SemiBold"
                                                   Foreground="{StaticResource TextPrimary}" HorizontalAlignment="Center"
                                                   Margin="0,0,0,16"/>

                                        <!-- Upload File card -->
                                        <Button Command="{Binding UploadLogCommand}" Cursor="Hand" Margin="0,0,0,10" Padding="0">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="Bg" Background="{StaticResource BgSecondary}"
                                                            BorderBrush="{StaticResource BorderDefault}" BorderThickness="1"
                                                            CornerRadius="10" Padding="20,16">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="48"/>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <Border Grid.Column="0" Background="{StaticResource AccentSubtle}"
                                                                    CornerRadius="8" Width="40" Height="40" HorizontalAlignment="Left">
                                                                <materialDesign:PackIcon Kind="Upload" Width="20" Height="20"
                                                                                         Foreground="{StaticResource AccentPrimary}"
                                                                                         HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                                <TextBlock Text="Upload a log file" FontSize="14" FontWeight="SemiBold"
                                                                           Foreground="{StaticResource TextPrimary}"/>
                                                                <TextBlock Text="Open a .log file from your computer — instant analysis"
                                                                           FontSize="12" Foreground="{StaticResource TextMuted}" Margin="0,3,0,0"/>
                                                            </StackPanel>
                                                            <materialDesign:PackIcon Grid.Column="2" Kind="ChevronRight" Width="18" Height="18"
                                                                                     Foreground="{StaticResource TextMuted}" VerticalAlignment="Center"/>
                                                        </Grid>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="Bg" Property="Background" Value="{StaticResource BgTertiary}"/>
                                                            <Setter TargetName="Bg" Property="BorderBrush" Value="{StaticResource AccentPrimary}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>

                                        <!-- Load Folder card -->
                                        <Button Command="{Binding LoadLogFolderCommand}" Cursor="Hand" Margin="0,0,0,10" Padding="0">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="Bg" Background="{StaticResource BgSecondary}"
                                                            BorderBrush="{StaticResource BorderDefault}" BorderThickness="1"
                                                            CornerRadius="10" Padding="20,16">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="48"/>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <Border Grid.Column="0" Background="{StaticResource AccentSubtle}"
                                                                    CornerRadius="8" Width="40" Height="40" HorizontalAlignment="Left">
                                                                <materialDesign:PackIcon Kind="FolderOpen" Width="20" Height="20"
                                                                                         Foreground="{StaticResource AccentPrimary}"
                                                                                         HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                                <TextBlock Text="Load a folder" FontSize="14" FontWeight="SemiBold"
                                                                           Foreground="{StaticResource TextPrimary}"/>
                                                                <TextBlock Text="Scan a folder — see all logs grouped by transaction"
                                                                           FontSize="12" Foreground="{StaticResource TextMuted}" Margin="0,3,0,0"/>
                                                            </StackPanel>
                                                            <materialDesign:PackIcon Grid.Column="2" Kind="ChevronRight" Width="18" Height="18"
                                                                                     Foreground="{StaticResource TextMuted}" VerticalAlignment="Center"/>
                                                        </Grid>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="Bg" Property="Background" Value="{StaticResource BgTertiary}"/>
                                                            <Setter TargetName="Bg" Property="BorderBrush" Value="{StaticResource AccentPrimary}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>

                                        <!-- Go Live card —— green border, stands out -->
                                        <Button Command="{Binding ToggleStreamingCommand}" Cursor="Hand" Margin="0,0,0,32" Padding="0">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="Bg" Background="{StaticResource SuccessSubtle}"
                                                            BorderBrush="{StaticResource Success}" BorderThickness="1"
                                                            CornerRadius="10" Padding="20,16">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="48"/>
                                                                <ColumnDefinition Width="*"/>
                                                                <ColumnDefinition Width="Auto"/>
                                                            </Grid.ColumnDefinitions>
                                                            <Border Grid.Column="0" Background="{StaticResource SuccessSubtle}"
                                                                    CornerRadius="8" Width="40" Height="40" HorizontalAlignment="Left">
                                                                <materialDesign:PackIcon Kind="AccessPoint" Width="20" Height="20"
                                                                                         Foreground="{StaticResource Success}"
                                                                                         HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                                <StackPanel Orientation="Horizontal">
                                                                    <TextBlock Text="Go Live" FontSize="14" FontWeight="SemiBold"
                                                                               Foreground="{StaticResource Success}"/>
                                                                    <Border Background="{StaticResource SuccessSubtle}" CornerRadius="4"
                                                                            Padding="6,2" Margin="8,0,0,0" VerticalAlignment="Center">
                                                                        <TextBlock Text="REAL-TIME" FontSize="9" FontWeight="Bold"
                                                                                   Foreground="{StaticResource Success}"/>
                                                                    </Border>
                                                                </StackPanel>
                                                                <TextBlock Text="Stream logs from your org as users interact with it"
                                                                           FontSize="12" Foreground="{StaticResource TextMuted}" Margin="0,3,0,0"/>
                                                            </StackPanel>
                                                            <materialDesign:PackIcon Grid.Column="2" Kind="ChevronRight" Width="18" Height="18"
                                                                                     Foreground="{StaticResource Success}" VerticalAlignment="Center"/>
                                                        </Grid>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="Bg" Property="Background" Value="{StaticResource BgHover}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>

                                        <!-- Drag and drop hint -->
                                        <TextBlock Text="or drag &amp; drop a .log file anywhere on this window"
                                                   FontSize="12" Foreground="{StaticResource TextMuted}"
                                                   HorizontalAlignment="Center" Margin="0,0,0,40"/>
                                    </StackPanel>
                                </ScrollViewer>
                            </Border>

                            <!-- Skeleton Loader (shown while log is being parsed) -->
                            <Border Panel.ZIndex="10" Background="{StaticResource BgPrimary}" CornerRadius="8"
                                    Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel Margin="24" VerticalAlignment="Top">
                                    <Rectangle Height="48" Style="{StaticResource SkeletonLoader}" Margin="0,0,0,12"/>
                                    <Rectangle Height="24" Width="200" HorizontalAlignment="Left" Style="{StaticResource SkeletonLoader}" Margin="0,0,0,8"/>
                                    <UniformGrid Columns="4" Margin="0,0,0,12">
                                        <Rectangle Height="80" Style="{StaticResource SkeletonLoader}" Margin="0,0,8,0"/>
                                        <Rectangle Height="80" Style="{StaticResource SkeletonLoader}" Margin="0,0,8,0"/>
                                        <Rectangle Height="80" Style="{StaticResource SkeletonLoader}" Margin="0,0,8,0"/>
                                        <Rectangle Height="80" Style="{StaticResource SkeletonLoader}"/>
                                    </UniformGrid>
                                    <Rectangle Height="120" Style="{StaticResource SkeletonLoader}" Margin="0,0,0,12"/>
                                    <Rectangle Height="80" Style="{StaticResource SkeletonLoader}" Margin="0,0,0,8"/>
                                    <Rectangle Height="80" Style="{StaticResource SkeletonLoader}"/>
                                </StackPanel>
                            </Border>

                            <!-- First-Log Onboarding Tour Overlay (shown on first analysis, dismissed to settings) -->
                            <Border Background="#CC000000" Panel.ZIndex="9"
                                    Visibility="{Binding ShowOnboardingTour, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Border Background="{StaticResource BgSecondary}" CornerRadius="12" Padding="28,24"
                                        Width="480" HorizontalAlignment="Center" VerticalAlignment="Center"
                                        BorderBrush="{StaticResource AccentPrimary}" BorderThickness="2">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                            <TextBlock Text="👋" FontSize="24" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="Welcome to Black Widow!" FontSize="20" FontWeight="Bold"
                                                       Foreground="White" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Text="Here's how to read your first log — takes 30 seconds."
                                                   FontSize="13" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,20" TextWrapping="Wrap"/>

                                        <!-- Step 1 -->
                                        <Border Background="{StaticResource BgTertiary}" CornerRadius="8" Padding="14,12" Margin="0,0,0,10">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0" Background="{StaticResource AccentPrimary}" CornerRadius="16" Width="32" Height="32" Margin="0,0,12,0">
                                                    <TextBlock Text="1" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <StackPanel Grid.Column="1">
                                                    <TextBlock Text="Summary tab = quick health check" FontWeight="SemiBold" FontSize="13" Foreground="White"/>
                                                    <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="{StaticResource TextSecondary}" Margin="0,4,0,0"
                                                               Text="The score and color tell you everything at a glance. 🟢 Green = fine, 🟡 Yellow = watch it, 🔴 Red = fix this."/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>

                                        <!-- Step 2 -->
                                        <Border Background="{StaticResource BgTertiary}" CornerRadius="8" Padding="14,12" Margin="0,0,0,10">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0" Background="{StaticResource AccentPrimary}" CornerRadius="16" Width="32" Height="32" Margin="0,0,12,0">
                                                    <TextBlock Text="2" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <StackPanel Grid.Column="1">
                                                    <TextBlock Text="Explain tab = plain English" FontWeight="SemiBold" FontSize="13" Foreground="White"/>
                                                    <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="{StaticResource TextSecondary}" Margin="0,4,0,0"
                                                               Text="Click the Explain tab to see a plain-English story of what your code did — no Apex knowledge required."/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>

                                        <!-- Step 3 -->
                                        <Border Background="{StaticResource BgTertiary}" CornerRadius="8" Padding="14,12" Margin="0,0,0,20">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0" Background="{StaticResource AccentPrimary}" CornerRadius="16" Width="32" Height="32" Margin="0,0,12,0">
                                                    <TextBlock Text="3" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <StackPanel Grid.Column="1">
                                                    <TextBlock Text="Use the navigator bar (top of Summary)" FontWeight="SemiBold" FontSize="13" Foreground="White"/>
                                                    <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="{StaticResource TextSecondary}" Margin="0,4,0,0"
                                                               Text="Click what you're investigating — 'something is slow', 'something crashed', etc. — to jump straight to the right view."/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>

                                        <Button Content="Got it — let me explore!"
                                                Command="{Binding DismissOnboardingTourCommand}"
                                                HorizontalAlignment="Right"
                                                Background="{StaticResource AccentPrimary}" Foreground="White"
                                                BorderThickness="0" Padding="20,10" FontSize="13" FontWeight="SemiBold"
                                                Cursor="Hand"/>
                                    </StackPanel>
                                </Border>
                            </Border>


                            <!-- Tab Content — UserControls -->
                            <tabs:SummaryTab Visibility="{Binding SelectedTabIndex, Converter={StaticResource IntEqualsToVisibilityConverter}, ConverterParameter=0}"/>
                            <tabs:TreeTab Visibility="{Binding SelectedTabIndex, Converter={StaticResource IntEqualsToVisibilityConverter}, ConverterParameter=1}"/>
                            <tabs:TimelineTab Visibility="{Binding SelectedTabIndex, Converter={StaticResource IntEqualsToVisibilityConverter}, ConverterParameter=2}"/>
                            <tabs:QueriesTab Visibility="{Binding SelectedTabIndex, Converter={StaticResource IntEqualsToVisibilityConverter}, ConverterParameter=3}"/>
                            <tabs:ExplainTab Visibility="{Binding SelectedTabIndex, Converter={StaticResource IntEqualsToVisibilityConverter}, ConverterParameter=4}"/>
                            <tabs:DebugTab Visibility="{Binding SelectedTabIndex, Converter={StaticResource IntEqualsToVisibilityConverter}, ConverterParameter=5}"/>
                            <tabs:SecurityTab Visibility="{Binding SelectedTabIndex, Converter={StaticResource IntEqualsToVisibilityConverter}, ConverterParameter=6}"/>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>

            <!-- Alert Center Overlay (right-aligned slide-out panel) -->
            <views:AlertCenterPanel x:Name="AlertCenterPanel"
                                     HorizontalAlignment="Right" VerticalAlignment="Stretch"
                                     Panel.ZIndex="10"
                                     Visibility="{Binding ShowAlertCenter, Converter={StaticResource BooleanToVisibilityConverter}}">
                <views:AlertCenterPanel.RenderTransform>
                    <TranslateTransform X="400"/>
                </views:AlertCenterPanel.RenderTransform>
                <views:AlertCenterPanel.Style>
                    <Style TargetType="views:AlertCenterPanel">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ShowAlertCenter}" Value="True">
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(TranslateTransform.X)"
                                                             From="400" To="0" Duration="0:0:0.25">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </views:AlertCenterPanel.Style>
            </views:AlertCenterPanel>
        </Grid>
        
        <!-- ═══ BOTTOM STATUS BAR ═══ -->
        <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                Background="{StaticResource SidebarBg}"
                BorderBrush="{StaticResource SidebarBorder}" BorderThickness="0,1,0,0">
            <Grid Margin="12,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <!-- Left: status message -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="InformationOutline" Width="12" Height="12"
                                             Foreground="{StaticResource TextMuted}" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBlock Text="{Binding StatusMessage}" FontSize="11"
                               Foreground="{StaticResource TextMuted}" VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"/>
                </StackPanel>
                <!-- Right: tier + version -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,4,0">
                    <Border CornerRadius="3" Padding="6,2" Margin="0,0,8,0">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="{StaticResource BgHover}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding LicenseStatus}" Value="Pro">
                                        <Setter Property="Background" Value="{StaticResource AccentSubtle}"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding LicenseStatus}" Value="Trial">
                                        <Setter Property="Background" Value="{StaticResource WarningSubtle}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock FontSize="10" FontWeight="Bold">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="FREE"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding LicenseStatus}" Value="Pro">
                                            <Setter Property="Text" Value="✦ PRO"/>
                                            <Setter Property="Foreground" Value="{StaticResource AccentPrimary}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding LicenseStatus}" Value="Trial">
                                            <Setter Property="Text" Value="TRIAL"/>
                                            <Setter Property="Foreground" Value="{StaticResource Warning}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Border>
                    <TextBlock Text="Black Widow v1.0" FontSize="11" Foreground="{StaticResource TextMuted}" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ===== TOAST NOTIFICATION (floating top-right, auto-dismiss) ===== -->
        <Border HorizontalAlignment="Right" VerticalAlignment="Top"
                Margin="0,60,24,0" MaxWidth="380" Panel.ZIndex="99"
                Visibility="{Binding IsToastVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
                CornerRadius="8" BorderThickness="1">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="{StaticResource BgSecondary}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource AccentPrimary}"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ToastType}" Value="success">
                            <Setter Property="BorderBrush" Value="{StaticResource Success}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ToastType}" Value="error">
                            <Setter Property="BorderBrush" Value="{StaticResource Danger}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ToastType}" Value="warning">
                            <Setter Property="BorderBrush" Value="{StaticResource Warning}"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="4"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <!-- Colored left accent bar -->
                <Border Grid.Column="0" CornerRadius="8,0,0,8">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Background" Value="{StaticResource AccentPrimary}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ToastType}" Value="success">
                                    <Setter Property="Background" Value="{StaticResource Success}"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding ToastType}" Value="error">
                                    <Setter Property="Background" Value="{StaticResource Danger}"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding ToastType}" Value="warning">
                                    <Setter Property="Background" Value="{StaticResource Warning}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                </Border>
                <!-- Message -->
                <TextBlock Grid.Column="1" Text="{Binding ToastMessage}" Padding="12,10"
                           FontSize="13" Foreground="{StaticResource TextPrimary}"
                           VerticalAlignment="Center" TextWrapping="Wrap"/>
                <!-- Dismiss button -->
                <Button Grid.Column="2" Command="{Binding DismissToastCommand}"
                        Background="Transparent" BorderThickness="0" Padding="8,4" Cursor="Hand"
                        VerticalAlignment="Center" Margin="0,0,6,0">
                    <materialDesign:PackIcon Kind="Close" Width="14" Height="14"
                                             Foreground="{StaticResource TextMuted}"/>
                </Button>
            </Grid>
        </Border>

        <!-- ===== COMMAND PALETTE (Ctrl+K) ===== -->
        <Border Background="#AA000000" Panel.ZIndex="100"
                Visibility="{Binding IsCommandPaletteOpen, Converter={StaticResource BooleanToVisibilityConverter}}"
                MouseDown="CommandPaletteBackdrop_MouseDown">
            <Border Background="{StaticResource BgSecondary}" CornerRadius="12"
                    Width="620" MaxHeight="520" VerticalAlignment="Top" Margin="0,80,0,0"
                    BorderBrush="{StaticResource AccentPrimary}" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <!-- Search box -->
                    <Border Grid.Row="0" BorderBrush="{StaticResource BorderDefault}" BorderThickness="0,0,0,1" Padding="14,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <materialDesign:PackIcon Kind="Magnify" Width="18" Height="18"
                                                     Foreground="{StaticResource TextMuted}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBox Grid.Column="1" x:Name="CommandPaletteBox"
                                     Text="{Binding CommandPaletteQuery, UpdateSourceTrigger=PropertyChanged}"
                                     Background="Transparent" BorderThickness="0"
                                     FontSize="14" Foreground="{StaticResource TextPrimary}"
                                     Padding="0" CaretBrush="{StaticResource AccentPrimary}"
                                     TextChanged="CommandPaletteBox_TextChanged"
                                     KeyDown="CommandPaletteBox_KeyDown"
                                     VerticalContentAlignment="Center"/>
                        </Grid>
                    </Border>
                    <!-- Results list -->
                    <ListBox Grid.Row="1" x:Name="PaletteResultsList"
                             ItemsSource="{Binding PaletteResults}"
                             Background="Transparent" BorderThickness="0"
                             MaxHeight="380" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="14,10"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource BgHover}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{StaticResource AccentSubtle}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="32"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <materialDesign:PackIcon Kind="{Binding Icon}" Width="16" Height="16"
                                                             Foreground="{StaticResource AccentPrimary}"
                                                             VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding Title}" FontSize="13" Foreground="{StaticResource TextPrimary}" FontWeight="Medium"/>
                                        <TextBlock Text="{Binding Description}" FontSize="11" Foreground="{StaticResource TextMuted}" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <!-- Keyboard hints -->
                    <Border Grid.Row="2" BorderBrush="{StaticResource BorderDefault}" BorderThickness="0,1,0,0" Padding="14,8">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock FontSize="11" Foreground="{StaticResource TextMuted}">
                                <Run Text="↵ run"/> <Run Text=" ·  " Foreground="{StaticResource BorderDefault}"/>
                                <Run Text="↑↓ navigate"/> <Run Text=" ·  " Foreground="{StaticResource BorderDefault}"/>
                                <Run Text="Esc close"/> <Run Text=" ·  " Foreground="{StaticResource BorderDefault}"/>
                                <Run Text="Ctrl+K toggle"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Border>

    </Grid>
</Window>
