<UserControl x:Class="SalesforceDebugAnalyzer.Views.AlertCenterPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Border Background="{StaticResource BgSecondary}" CornerRadius="12,0,0,12"
            BorderBrush="{StaticResource BorderDefault}" BorderThickness="1,1,0,1"
            Width="380">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Padding="16,12"
                    BorderBrush="{StaticResource BorderDefault}" BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Alert Center"
                               FontSize="16" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimary}"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="Mark All Read" Click="MarkAllRead_Click"
                                Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"
                                Foreground="{StaticResource AccentPrimary}" FontSize="11"
                                Background="Transparent" BorderThickness="0" Padding="8,4"
                                Margin="0,0,4,0"/>
                        <Button Click="Close_Click"
                                Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"
                                Background="Transparent" BorderThickness="0" Width="28" Height="28">
                            <materialDesign:PackIcon Kind="Close"
                                                     Foreground="{StaticResource TextSecondary}" Width="14" Height="14"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Severity Filter -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="12,8">
                <RadioButton Content="All" IsChecked="True" GroupName="SeverityFilter"
                             Checked="FilterAll_Checked"
                             Style="{StaticResource MaterialDesignChoiceChipRadioButton}"
                             Margin="0,0,4,0"/>
                <RadioButton Content="Critical" GroupName="SeverityFilter"
                             Checked="FilterCritical_Checked"
                             Style="{StaticResource MaterialDesignChoiceChipRadioButton}"
                             Margin="0,0,4,0"/>
                <RadioButton Content="Warning" GroupName="SeverityFilter"
                             Checked="FilterWarning_Checked"
                             Style="{StaticResource MaterialDesignChoiceChipRadioButton}"
                             Margin="0,0,4,0"/>
            </StackPanel>

            <!-- Alert List -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="AlertList" Padding="8,4">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource BgTertiary}" CornerRadius="8"
                                    Margin="4,3" Padding="12,10"
                                    BorderBrush="{StaticResource BorderDefault}" BorderThickness="1"
                                    Opacity="{Binding Opacity}"
                                    Cursor="Hand"
                                    MouseDown="AlertItem_MouseDown"
                                    Tag="{Binding}">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{StaticResource BgHover}"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="4"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Severity color bar -->
                                    <Border Grid.Column="0" CornerRadius="2"
                                            Width="4" Margin="0,0,10,0"
                                            VerticalAlignment="Stretch">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="{StaticResource TextMuted}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Severity}" Value="critical">
                                                        <Setter Property="Background" Value="{StaticResource Danger}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Severity}" Value="warning">
                                                        <Setter Property="Background" Value="{StaticResource Warning}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                    </Border>

                                    <!-- Alert content -->
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding Title}" TextWrapping="Wrap"
                                                   FontSize="12" FontWeight="Medium"
                                                   Foreground="{StaticResource TextPrimary}"/>
                                        <TextBlock Text="{Binding Description}" TextWrapping="Wrap"
                                                   FontSize="11" Margin="0,4,0,0" MaxHeight="44"
                                                   Foreground="{StaticResource TextSecondary}"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                            <TextBlock Text="{Binding TimeAgo}" FontSize="10"
                                                       Foreground="{StaticResource TextMuted}"/>
                                            <TextBlock Text=" · " FontSize="10"
                                                       Foreground="{StaticResource TextMuted}"/>
                                            <TextBlock Text="{Binding EntryPoint}" FontSize="10"
                                                       Foreground="{StaticResource AccentPrimary}"
                                                       TextTrimming="CharacterEllipsis" MaxWidth="180"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <!-- Dismiss button -->
                                    <Button Grid.Column="2" VerticalAlignment="Top"
                                            Click="DismissAlert_Click"
                                            Tag="{Binding Id}"
                                            Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"
                                            Background="Transparent" BorderThickness="0"
                                            Width="24" Height="24" Margin="4,0,0,0">
                                        <materialDesign:PackIcon Kind="Close"
                                                                 Foreground="{StaticResource TextMuted}"
                                                                 Width="12" Height="12"/>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Empty state -->
            <StackPanel x:Name="EmptyState" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center"
                        Visibility="Visible">
                <materialDesign:PackIcon Kind="BellCheckOutline" Width="48" Height="48"
                                         Foreground="{StaticResource TextMuted}"
                                         HorizontalAlignment="Center" Margin="0,0,0,12"/>
                <TextBlock Text="No alerts" FontSize="14"
                           Foreground="{StaticResource TextMuted}"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Monitoring will surface issues as they're detected"
                           FontSize="12" Foreground="{StaticResource TextMuted}"
                           HorizontalAlignment="Center" Margin="0,4,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
